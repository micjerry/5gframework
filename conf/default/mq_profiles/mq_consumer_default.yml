name: "default"
type: "consumer"
connections:
    -
      hostname    : "127.0.0.1"
      virtualhost : "/"
      username    : "guest"
      password    : "guest"
      port        : "5672"
      heartbeat   : "0"

parameters:
    exname: "TAP.Commands"
    binding_key: "agc.command"
    reconnect_interval_ms : "1000"
    